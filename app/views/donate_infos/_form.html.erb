<%= simple_form_for(@donate_info) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :currency %>
    <%= f.input :name %>
    <%= f.input :address %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
